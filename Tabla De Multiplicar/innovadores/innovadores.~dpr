program innovadores;

uses
  Forms,
  main in '..\Form\main.pas' {mainform},
  matriculas in '..\Form\matriculas.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(Tmainform, mainform);
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
